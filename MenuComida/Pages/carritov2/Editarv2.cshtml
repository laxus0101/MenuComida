@page
@model MenuComida.Pages.carritov2.Editarv2Model

@{
    ViewData["Title"] = "Editarv2";
}

<h1>Editarv2</h1>

<h4>carrito</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="carrito.idpedido" />
          
            <input asp-for="carrito.producto" class="form-control" />

             <input asp-for="carrito.imagen" class="form-control" />

            <div class="form-group">
                <label asp-for="carrito.cantidad" class="control-label"></label>
                <input asp-for="carrito.cantidad" class="form-control" />
                <span asp-validation-for="carrito.cantidad" class="text-danger"></span>
            </div>

 
             <input asp-for="carrito.precio" class="form-control" />

             <input asp-for="carrito.idUsuario" class="form-control" />

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
